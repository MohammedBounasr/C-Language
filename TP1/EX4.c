#include <stdio.h>

int main()
{
    int num,f;
    printf("enter  a nomber ");
    scanf("%d", &num);
    for (int i = 2; i <= num; i++)
    {
        if (i==0 || i==1)
        {
           f=1:

        }
        else
        {
            f=f-1+f-2;
        }
        
        
        
    }

    return 0;
}